name: Deploy to Infomaniak

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 57-107596.ssh.hosting-ik.com
          username: j6tLfqHN8K4_deploy
          password: ${{ secrets.INFOMANIAKLOGIN }}
          command_timeout: 5m
          script: |
            cd /srv/customer/sites/yossi.eu
            curl -sL -H "Authorization: token ${{ secrets.TOKEN }}" https://api.github.com/repos/yossibokorbleile/yossi-eu-deploy/tarball/main -o /tmp/deploy.tar.gz
            tar xzf /tmp/deploy.tar.gz --strip-components=1 -C /srv/customer/sites/yossi.eu
            rm /tmp/deploy.tar.gz
            npm install --production
